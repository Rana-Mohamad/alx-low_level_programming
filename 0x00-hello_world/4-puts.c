#include <stdio.h>
#include <string.h>
/**
   *main = entry point
   *return: 0 by default (success)
 */
int main(void)
{
	char str1[] = "\"Programming is like building a multilingual puzzle";

	puts(str1);
	return (0);
}
