#include "main.h"

/**
  * _isalpha - checks if the character is a letter
  * @c: checks input
  * Return: always 0, but it returns 1 if c is a letter, lowercase or uppercase
  */

int _isalpha(int c)
{
	if (c >= 65 && C <= 122)
		return (1);
	return (0);
}
