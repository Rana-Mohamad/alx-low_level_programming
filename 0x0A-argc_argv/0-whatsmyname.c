#include <stdlib.h>
#include <stdio.h>

/**
 * main - Entry point
 * @argc: argument counter
 * @argv: argument array
 * Return: always 0 (Success)
 */

int main(int __attribute__((unused)) argc, char *argv[])
{
	int i = 0;

	printf("%s \n", argv[i]);
	return (0);
}
